# This is a DAG, not magic filenames

[[step]]
name = "extract"
script = '''
echo "hello world" > $OUTPUT_DIR/process
'''

[[step]]
name = "process"
inputs = ["process"]
script = '''
tr '[:lower:]' '[:upper:]' < $INPUT_FILE > $OUTPUT_DIR/done
'''
